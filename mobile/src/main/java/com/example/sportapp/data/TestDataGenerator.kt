package com.example.sportapp.data

/**
 * TestDataGenerator has been removed as part of the cleanup process.
 * Developer mode and CSV-based test data are no longer supported.
 */
object TestDataGenerator {
    // Empty implementation to avoid breaking potential remaining references during migration
}
