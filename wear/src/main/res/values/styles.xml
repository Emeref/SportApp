<resources>

    <style name="MainActivityTheme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault">
        <!-- Flagi windowShowWhenLocked i windowTurnScreenOn wymagają API 27+ w stylu,
             używamy ich bezpośrednio w kodzie lub sprawdzamy atrybuty bez prefiksu android: w nowszych SDK -->
    </style>
</resources>